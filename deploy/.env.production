# SmartMix 生产环境配置
# 部署前请务必修改所有 CHANGE_ME_IN_PRODUCTION 值

# ===== 数据库配置 =====
# 如果使用阿里云 RDS，填写 RDS 连接信息
# 如果使用自建 MySQL，填写 ECS 上的 MySQL 连接信息
DB_HOST=CHANGE_ME_IN_PRODUCTION
DB_PORT=3306
DB_NAME=smartmix
DB_USERNAME=CHANGE_ME_IN_PRODUCTION
DB_PASSWORD=CHANGE_ME_IN_PRODUCTION

# MySQL JDBC URL（通常不需要修改，会根据上面的变量自动生成）
SPRING_DATASOURCE_URL=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?useSSL=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8

# ===== Redis 配置 =====
# 如果使用阿里云 Redis，填写 Redis 连接信息
# 如果使用自建 Redis，填写 ECS 上的 Redis 连接信息
REDIS_HOST=CHANGE_ME_IN_PRODUCTION
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_ME_IN_PRODUCTION

# ===== 应用安全配置 =====
# JWT 密钥（必须是强随机字符串，至少 32 位）
# 生成方式: openssl rand -base64 32
APP_JWT_SECRET=CHANGE_ME_IN_PRODUCTION_USE_STRONG_RANDOM_STRING_MIN_32_CHARS

# JWT 过期时间（小时）
APP_JWT_EXPIRATION_HOURS=24

# ERP Webhook Token（用于 ERP 系统调用接口时验证）
# 生成方式: openssl rand -base64 32
ERP_WEBHOOK_TOKEN=CHANGE_ME_IN_PRODUCTION_USE_STRONG_RANDOM_STRING

# ===== 服务器配置 =====
SERVER_PORT=8080

# ===== 日志配置 =====
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_COM_SMARTMIX=INFO

# ===== 文件上传配置 =====
# PDF 文件存储路径（容器内路径）
FILE_UPLOAD_PATH=/app/uploads

# 最大文件上传大小（MB）
SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE=10MB
SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE=10MB

# ===== CORS 配置 =====
# 允许的前端域名（如果有域名，填写域名；如果是 IP，填写 IP）
# 多个域名用逗号分隔
CORS_ALLOWED_ORIGINS=http://localhost,http://127.0.0.1,http://CHANGE_ME_TO_YOUR_DOMAIN_OR_IP

# ===== 数据库连接池配置 =====
SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE=10
SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE=5

# ===== 其他配置 =====
# 时区
TZ=Asia/Shanghai
